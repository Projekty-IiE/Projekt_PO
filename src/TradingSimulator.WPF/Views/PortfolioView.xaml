<UserControl x:Class="TradingSimulator.WPF.Views.PortfolioView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Your Portfolio" 
                   FontSize="20" 
                   FontWeight="Bold" 
                   Margin="0,0,0,10"/>

        <DataGrid Grid.Row="1" 
                  ItemsSource="{Binding PortfolioItems}" 
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  Background="White">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Symbol" Binding="{Binding Stock.Symbol}" Width="80" FontWeight="Bold"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Stock.Name}" Width="120" FontWeight="SemiBold" Foreground="Gray"/>
                <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="80"/>
                <DataGridTextColumn Header="Current Price" Binding="{Binding Stock.Price, StringFormat=C2}" Width="100"/>

                <DataGridTextColumn Header="Profit/Loss" 
                                    Binding="{Binding UnrealizedPnL, StringFormat=C2}" 
                                    Width="100"
                                    FontWeight="Bold"/>

                <DataGridTextColumn Header="Total Value" Binding="{Binding TotalValue, StringFormat=C2}" Width="*" FontWeight="Bold" Foreground="Green"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>