<Window x:Class="TradingSimulator.WPF.Views.TransactionHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Transaction History"
        Height="450"
        Width="800">

    <Grid Margin="10">
        <DataGrid ItemsSource="{Binding Transactions}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Time" Binding="{Binding Time}" Width="150"/>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="80"/>
                <DataGridTextColumn Header="Symbol" Binding="{Binding StockSymbol}" Width="80"/>
                <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="80"/>
                <DataGridTextColumn Header="Price" Binding="{Binding PricePerShare, StringFormat=C2}" Width="100"/>
                <DataGridTextColumn Header="PnL" Binding="{Binding RealizedPnL, StringFormat=C2}" Width="*"/>
            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</Window>
